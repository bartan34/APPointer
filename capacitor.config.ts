import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'APPointer.project',
  appName: 'APPointer',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
